[phases.setup]
nixPkgs = ["python310"]

[phases.install]
cmds = ["cd services/voice-agent && pip install -r requirements.txt"]

[start]
cmd = "cd services/voice-agent && python src/main.py --mode both"

