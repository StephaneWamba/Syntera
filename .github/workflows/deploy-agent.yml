name: CI/CD Agent Service

on:
  push:
    paths:
      - 'services/agent/**'
      - 'shared/**'
    branches:
      - main
      - develop
  pull_request:
    paths:
      - 'services/agent/**'
      - 'shared/**'
    branches:
      - main
      - develop
  workflow_dispatch:

env:
  NODE_VERSION: '20'
  PNPM_VERSION: '8.15.0'

jobs:
  build-and-test:
    name: Build and Test
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build shared package
        run: |
          echo "Building shared package..."
          pnpm --filter @syntera/shared build
          echo "Shared package build completed"
          ls -la shared/dist/ | head -5 || echo "Shared dist not found"

      - name: Type check agent service
        run: |
          cd services/agent
          echo "Running type check..."
          pnpm exec tsc --noEmit
          echo "Type check passed"

      - name: Build agent service
        working-directory: services/agent
        run: |
          echo "Building agent service..."
          echo "Current directory: $(pwd)"
          pnpm build
          echo "Build command completed"
          echo "Checking build output..."
          ls -la dist/ | head -10 || echo "No dist directory found"
          echo "Checking for index.js..."
          ls -la dist/index.js || echo "index.js not found in dist/"

      - name: Verify build output
        run: |
          echo "Checking build output..."
          echo "Current directory: $(pwd)"
          echo "Checking if dist/index.js exists..."
          if [ -f "services/agent/dist/index.js" ]; then
            echo "✅ Build successful: dist/index.js found"
            echo "File size: $(stat -c%s services/agent/dist/index.js 2>/dev/null || echo 'unknown')"
            echo "Dist directory contents:"
            ls -la services/agent/dist/ | head -10
          else
            echo "❌ Build failed: dist/index.js not found at services/agent/dist/index.js"
            echo "Checking actual dist structure:"
            find services/agent/dist -name "index.js" -type f 2>/dev/null || echo "No index.js found anywhere in dist"
            echo "Checking dist directory structure:"
            find services/agent/dist -type f -name "*.js" | head -10
            echo "Checking if files are in wrong location:"
            find services/agent -name "index.js" -type f 2>/dev/null | head -5
            exit 1
          fi

